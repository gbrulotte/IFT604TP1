package commands;

import java.net.DatagramPacket;

import matchServer.Client;
import matchServer.ListeDesMatchs;
import matchServer.Match;

/*public class SuivreMatchCommand implements ICommand{
	
	Client client = null;
	String input= null;
	
	public SuivreMatchCommand(Client client, String input){
		this.client = client;
		this.input = input;
	}
	
	@Override
	public void execute(){
		Match a = ListeDesMatchs.matches.get(Integer.parseInt(input));
		a.clients.add(client);
	}
}*/
